import { kawaii } from './kawaii';

/* THEMES_DISABLED — Re-enable when ready to add theme switching back:
import { ghibli } from './ghibli';
import { holographic } from './holographic';
import { terrarium } from './terrarium';

export const THEMES = { kawaii, ghibli, holographic, terrarium };
*/

export const THEMES = { kawaii };
export const DEFAULT_THEME = 'kawaii';
