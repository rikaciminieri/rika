/* THEMES_DISABLED — Kawaii uses WatermarkCanvas for cursor effects, not DOM dots.
   This component rendered cursor trails for ghibli/holographic/terrarium themes.
   When re-enabling themes, restore the DOM dot trail logic from git history. */

export default function CursorTrail() {
  return null;
}
